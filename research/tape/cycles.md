# loop cycles

| date | label | flag | decision | $/day a->b | green months a->b | H1 | H2 | trades | off book | on book | script |
|---|---|---|---|---|---|---|---|---|---|---|---|
| 2026-09-05 | the 1R first-target rule (superseded row, see correction below) | MIN_PT1_R | hold | -9.0 -> 29.0 | 12 -> 12 | pass | fail | 767 | book_MIN_PT1_R_off.json.gz | book_MIN_PT1_R_on.json.gz | research/loop_cycle.py |
<!-- L1 repair 2026-09-06 (referee pass 3, research/l1_referee.md commit 2dc6902d): the row above is full-29, priced before loop_cycle.py's apply_universe_filter landed (L2's referee repair) and before d062da84 moved the MIN_PT1_R gate below _apply_x_lift. Every other row in this table is core-11 on the current engine; this one was neither. Left in place rather than deleted -- it is what the loop controller actually printed on 2026-09-05 -- and corrected by the row immediately below it, re-derived via --stage gate --dry-run against the already-committed book_MIN_PT1_R_{off,on}_postfix.json.gz (no rebuild), core-11 filter applied, decision unchanged (hold, H2 dollar column fails: -$111 -> -$145). -->
| 2026-09-06 | the 1R first-target rule, core-11, post-x_lift-move (correction) | MIN_PT1_R | hold | -52.0 -> 28.0 | 11 -> 14 | pass | fail | 732 | book_MIN_PT1_R_off_postfix.json.gz | book_MIN_PT1_R_on_postfix.json.gz | research/loop_cycle.py --dry-run (research/l1_referee.md pass 3 verified this by independent script; not appended by the controller itself since --dry-run never mutates state) |
<!-- L2 referee repair 2026-09-05: two RULE84_DECIDED rows removed here. Both priced the unfiltered 28-symbol pool (loop_cycle.py did not apply loop.json's universe.row_filter) and the second was a duplicate append of the first. See research/l2_referee.md and the corrected row appended below by the fixed script. -->
| 2026-09-05 | the 84% re-entry as decided on the call | RULE84_DECIDED | hold | -52.0 -> -57.0 | 11 -> 11 | fail | pass | 769 | book_RULE84_DECIDED_off.json.gz | book_RULE84_DECIDED_on.json.gz | research/loop_cycle.py |
| 2026-09-05 | the one-candle-rule entry only when the retest candle is strong | OCR_RETEST_DISPLACEMENT | hold | -52.0 -> -58.0 | 11 -> 10 | fail | pass | 764 | book_OCR_RETEST_DISPLACEMENT_off.json.gz | book_OCR_RETEST_DISPLACEMENT_on.json.gz | research/loop_cycle.py |
| 2026-09-05 | the 15-minute structure trend test | TREND_DEF | hold | -52.0 -> -61.0 | 11 -> 10 | fail | pass | 768 | book_TREND_DEF_off.json.gz | book_TREND_DEF_on.json.gz | research/loop_cycle.py |
<!-- L4 repair 2026-09-05: a second, byte-identical TREND_DEF row (dropped) came from re-running --stage gate --dry-run just to re-print the same output, not a second experiment; it pushed consecutive_holds to 5 and tripped a false "stop" -- see loop_state.json's repair note. -->
| 2026-09-05 | up to three trades a day, stop after a win or two losses (repair) | DAY_POLICY | hold | -52.0 -> -52.0 | 11 -> 11 | pass | pass | 769 | book_DAY_POLICY_off.json.gz | book_DAY_POLICY_on.json.gz | research/loop_cycle.py |
<!-- L5 repair 2026-09-06 (referee pass 3, research/l5_referee.md): decision cell corrected from "ship" to "hold" -- pass 2 (34c1546e) instructed both the code-default revert and this cell's flip; only the revert landed. The number columns are unchanged (they always read no-op, both arms identical). -->
<!-- C1: Pin the backtest window so the baseline book id reproduces byte-for-byte - blocked (OFF-arm book id mismatch 2c39ced2697c26cc vs d5ba41a41d65e1e4; baseline archive gained 2 backfilled sessions post-stamp; economically immaterial ($/day -52->-52, 11/25 both) but fingerprint does not reproduce byte-for-byte; also found: loop_cycle.py smoke mode drops --end pin, book_stamp lacks session-set hash, untracked 132MB rebuild_c1_check.json risked AutoCommit push failure — code reverted, file deleted) -->
<!-- C2: The first-scale-point RR gate at three-quarters of an R instead of a full R - blocked (row not attempted: re-ran C1's already-blocked window-pin fix instead, same cause unsolved, 0 trades produced for MIN_PT1_R=0.75; referee also found the real mechanism -- polygon_feed.fetch_day() writes fetched days into data_archive/ on cache miss, so every backtest run self-poisons the next run's window; C2's own investigation added QQQ 2026-08-24/08-25 to the archive, widening the drift it was diagnosing; code reverted, no book stamped) -->
<!-- LAND C3: Enter while the 1-minute candle is still forming, and scratch if it closes back through the level - blocked (lookahead: forming branch reads completed-bar high/low; every detector requires close-through-level for selection; money is risk-denominator collapse on 17-trade sample, below 30-trade floor; stamp missing FORMING_ENTRY; not one change) -->
<!-- LAND C4: Stop requiring displacement on a break-and-retest - blocked (OFF-arm mismatch vs C1/C2, unresolved since morning: 25 of 28 archived symbols pinned at 2026-09-04, AMZN/QQQ at 2026-09-10, CRM at 2026-09-09; backtest_2y.py's window pin at line 156 takes max archive date across all symbols, so no flag row reproduces the 2024-09-04..2026-09-04 baseline until that is fixed; builder's 730-session rebuild re-confirmed what C1/C2 already established without needing to run; no ON book stamped, no on_value recorded for BNR_DISPLACEMENT_GATE; code reverted, no new files kept) -->
<!-- LAND C5: Break and retest only the six levels he actually watches - blocked (SIX_LEVELS_ONLY absent from book_stamp.py FLAG_SOURCES, so the kept OFF book is unstampable/indistinguishable from ON, violating law 5; no cycles.md trail was left originally; window drift worsened again -- OFF book_id dbf9c80d35d5e921 vs baseline 2c39ced2697c26cc, -767 rows, and this cycle's own smoke+build pushed QQQ/AMZN/CRM archive dates from 2026-09-09 to 2026-09-10, the same fetch_day() self-poisoning C1/C2/C4 already documented; no ON book built, no numbers claimed; code reverted, untracked OFF book deleted) -->
<!-- LAND P: engine fix (not a rule cycle) - fix the window pin so the loop's OFF arm reproduces a pinned baseline. Diagnosed: qqq_level_breaks(window) in backtest_2y.py called pf.fetch_day("QQQ", d) for every date in the cross-symbol union, not just QQQ's own archived days -- a cache miss there fetched-and-wrote the day, which is exactly what C2/C4/C5 already caught happening to them. Landed (commit 03ffa90a): backtest_2y.py --start/--end (explicit window, no longer derived from the archive's current last session); ARCHIVE_READONLY=1 in polygon_feed.fetch_day, set for backtest_2y.py's whole run, turns a cache miss into "no data" instead of a fetch; a shared research/tape/.rebuild_lock that also stops daily_fetch.py's scheduled 16:15 ET fetch from landing mid-rebuild; research/build_archive_manifest.py freezes every symbol's first/last session/count/hash (research/tape/archive_manifest_2026-09-13.json), and backtest_2y.py --manifest PATH refuses to build on drift unless --allow-drift. Rebuilt the OFF arm on the pinned window anyway: book_id d5ba41a41d65e1e4, NOT 2c39ced2697c26cc -- 501 sessions not 499 (AAPL's own calendar has only 497 in-window sessions; PLTR/QQQ/SPY/CRM/AMZN have since had 2026-08-12, 08-13, 08-24, 08-25 backfilled that AAPL still lacks). This is Fable's pre-ruled scenario: the window pin was necessary but the archive itself had moved inside it, so per the ruling the archive is frozen as of today and the loop re-baselined honestly on it rather than chasing a fingerprint that can no longer be produced. New baseline is economically identical to the old one (770 trades vs 769, $/day -52->-52, mean R -0.0335 both, 11/25 green both, both halves unchanged to the dollar) -- this row is plumbing and a re-baseline, not a trading-rule change, so no ship/hold verdict applies. loop.json's baseline_book/baseline_book_id/rebuild now point at baseline_2026-09-13 (book_id d5ba41a41d65e1e4); the 2026-09-05 baseline is kept side by side as baseline_book_2026_09_05/baseline_figures_2026_09_05, never deleted. research/g212_trace.py now correctly fails (it asserts the superseded 2026-09-05 config on purpose) -- research/p_pin_trace.py is the live equivalent. verify: regression_gate.py, test_runner_stop.py, test_universe_single_source.py all pass. -->
| 2026-09-13 | the first-scale-point RR gate at three-quarters of an R | MIN_PT1_R | hold | -52.0 -> 0.0 | 11 -> 14 | pass | fail | 741 | book_MIN_PT1_R_off.json.gz | book_MIN_PT1_R_on.json.gz | research/loop_cycle.py |
<!-- LAND C5: break and retest only the six levels he watches (PDH PDL PMH PML HOD LOD) - blocked (HOD/LOD are dead code in the ON arm -- signal_runner.py:3114's running-session-max hi/lo can never be broken by construction, so the row tests PDH/PDL/PMH/PML only, not six levels; 51% of the ON gain (+$36.5/day of +$123/day) comes from released order-block rows via the dedupe-release effect, not from the named levels; loop_state.json's stale stop:true/6-consecutive-holds reset to stop:false per Austin's card authorizing this run; code reverted, untracked books deleted, no numbers claimed) -->
<!-- LAND C3 (re-run 2026-09-13): enter while the 1-minute candle is still forming, scratch if it closes back through the level - blocked (referee: ON stamp does not carry ENTRY_FORMING_CANDLE in FLAG_SOURCES, both books stamp-identical; OFF/ON built from different base commits (8ef1222/a9e5442, AutoCommit landed signal_runner.py mid-build), violating law 5; builder's "selection untouched" claim is false -- core-11 status counts and whole-book signal count both shift between arms; of 480 shared unit rows only 18 changed entry price, carrying $16,078 of the $17,438 improvement (below the 30-trade floor, same defect that blocked the first C3 attempt); gain is risk-denominator collapse (stop distance shrinks, position size levers up) not a better rule, one-sided on 102/0 matched trades; branch books level+/-1-tick intra-bar, which is lookahead by this project's own prior ruling (g80_lookahead_refute.md) and bypasses fill_price's entry_fill delegation contract; the scratch half of the rule was never built (selection still requires close-through-level, so it's provably unreachable) -- only a fill-price substitution shipped, not the rule asked for. This result had already landed on main via AutoCommit (a9e5442c) and cycles.md/loop_state.json had already recorded it as a clean hold; that hold does not count and is corrected here. signal_runner.py reverted to 8ef12226 (ENTRY_FORMING_CANDLE flag and fill_price branch removed), loop_state.json reverted (cycle_count 8->7, consecutive_holds 1->0, cycle-8 history entry removed), both book_ENTRY_FORMING_CANDLE_{off,on}.json.gz deleted. No further row should price against these books; re-run from one base with the flag registered in book_stamp.py's FLAG_SOURCES if this rule is attempted again. -->

| 2026-09-14 | HOD/LOD as the prior bar's session extremes | HODLOD_DEF | hold | -52.0 -> -52.0 | 11 -> 11 | pass | pass | 770 | book_HODLOD_DEF_off.json.gz | book_HODLOD_DEF_on.json.gz | research/loop_cycle.py |
<!-- V6 referee repair 2026-09-14: removed a smoke-mode row (12 trades, -333.0->-333.0) that had polluted this ledger as cycle 8 in loop_state.json, and corrected the real cycle's decision from "ship" to "hold" -- byte-identical books (both arms -52.0/-52.0, 11/11 green, matching L5's DAY_POLICY precedent) are a no-op gate, not a measurement, per this project's standing rule. Two live default-path consumers of hod/lod were found that the builder's "risk-free by construction" claim missed: near_hod/near_lod (signal_runner.py:3446,3699) gated by RULE84_SOURCE (defaults False, so `not near_hod` is live; book holds 542 reentry_84_rule rows) and the first-session pdh/pdl fallback (signal_runner.py:3133-3134). Both arms were built from untracked research/tape/loop_v6_hodlod_tmp.json with --allow-drift, not the tracked loop.json; archive_manifest_2026-09-13.json needs re-freezing via research/build_archive_manifest.py before the next row. Code default (HODLOD_DEF=off) is unchanged, matching the pre-change behaviour. -->
