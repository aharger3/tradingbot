# P20/W6 — the sequence gate, OFF

Generated by `research/p20_sequence_gate.py`. Ships **OFF by default** in `research/downgrade.py` (`ENABLE_SEQUENCE_GATE = False`); every number below was produced by calling `downgrade.score` with `enable_sequence_gate=True`, which does not touch the module default any other caller sees. Nothing here is wired into detection.

Two rigs, same as `research/p2_threshold_sweep.md`: Austin's 120 graded day-cards (1250 signals) and `research/bt2y_trades.json` (45175 signals / 1016 traded).

## Does `NO_REPEAT_ENTRIES` already kill this?

**No.** `signal_runner.py::NO_REPEAT_ENTRIES` (default `True`, live) suppresses a repeat ACCEPTED signal only when it matches on symbol, direction, AND level (price rounded to a cent) — "a different [level] is a different idea and may still fire" (its own comment). Ballot b2's rule is broader: any subsequent graded entry on the symbol, any level, any direction. `NO_REPEAT_ENTRIES` removes only the narrowest slice of that population (exact-level repeats); the rest reaches `fired`/`traded` untouched. This is not the P15 shape — the population below is real.

## What counts as "an entry"

`downgrade.score()` grades every signal it is handed S/A/C — there is no skip grade in Austin's ladder — and every other caller in this codebase (`backtest_2y.py`, `t66_downgrade_measure.py`, P18/P19) already scores every DETECTED signal this way, legacy X/D/tight-stop status notwithstanding. So the primary reading here orders `entry_seq` over every detected signal on the symbol-day, not just the legacy engine's `fired` subset. The alternative (fired-only) reading is reported alongside as a sanity check — the two disagree sharply, which is worth seeing before trusting either number.

## Population, checked BEFORE the trip rate

| | cards (n=1250) | book, all-signal reading (n=45175) | book, fired-only reading (n=1393 fired) |
|---|---:|---:|---:|
| first entry (never trips) | 120 (9.6%) | 11803 (26.1%) | 1290 (92.6%) |
| 2nd+, 84%-rule EXEMPT | 0 (0.0%) | 3 (0.0%) | 3 (0.2%) |
| 2nd+, TRIPS | 1130 (90.4%) | 33369 (73.9%) | 100 (7.2%) |

The two readings disagree by nearly an order of magnitude on the book (73.9% vs 7.2% trip rate) — restricting "an entry" to what the legacy engine actually fired removes almost the whole population, because most 2nd+ detected signals on a symbol-day never reach `fired` at all (graded X/D, or filtered by `NO_REPEAT_ENTRIES` itself, or tight-stop). Neither reading is dead (0 trips) — see the money table below for which one actually separates R.

## Trip rate and money (traded book, n=1016)

| reading | trips on cards | trips on book | traded mean R tripped | clean | delta |
|---|---:|---:|---:|---:|---:|
| all-signal (primary) | 1130 (90.4%) | 33369 (73.9%) | +0.767R (n=422) | +1.092R (n=594) | -0.325R |
| fired-only (sanity check) | n/a | 100 (7.2%) | +1.247R (n=9) | +0.954R (n=1007) | +0.293R |

**The all-signal reading separates money, correctly signed and on a real sample**: tripped trades make +0.767R against +1.092R clean (delta -0.325R, n=422 tripped / 594 clean) — a subsequent entry really does make less, which is what a downgrade is supposed to find. The fired-only reading, by contrast, runs on only 9 traded signals and comes back wrong-signed — too small a sample to have an opinion, and the wrong sign besides.

## The number the ticket asks for: firsts vs. 2nd+ on the traded book

All-signal reading, n=1016 traded:

| | n | win | mean R |
|---|---:|---:|---:|
| firsts (entry_seq == 1) | n=591 | 57.9% | +1.087R |
| 2nd+, ALL | n=425 | 46.1% | +0.776R |
| 2nd+, 84%-rule EXEMPT | n=3 | 66.7% | +2.069R |
| 2nd+, TRIPS the downgrade | n=422 | 46.0% | +0.767R |

425 of the 1016 traded signals (41.8%) are 2nd-or-later on their symbol-day; 3 of those are the 84%-rule's own exemption, leaving 422 that would actually take this downgrade.

## Effect on the card gate and the S/A/C ladder, flag ON vs baseline

(All-signal reading; the fired-only reading has no matching card-side population to test against because `t66_downgrade_measure.replay` captures every signal, fired or not, the same as every other consumer of it.)

| setting | S recall | false fire | agree | day S/A/C |
|---|---|---|---|---|
| baseline (flag OFF) | 12/28 | 30/61 | 20/58 | 29/22/7 |
| `sequence_gate` ON | 8/28 | 9/61 | 22/58 | 16/20/22 |

| setting | S n | S win | S mean R | A mean R | C mean R | S>A>C |
|---|---:|---:|---:|---:|---:|---|
| baseline (flag OFF) | 129 | 66.7% | +1.313R | +1.018R | +0.862R | yes |
| `sequence_gate` ON | 77 | 75.3% | +1.572R | +1.004R | +0.881R | yes |

## The cap Austin has NOT settled — flagged, not implemented

Ballot c3: *"max S trades per symbol is 2"*; c4: *"max 3 s trades per symbol"* then *"cap at .8 s trades a day per symbol"* — three different numbers from Austin himself. This script and `research/downgrade.py` implement ONLY the quality downgrade (ballot b2); no hard cap on S trades per symbol is implemented anywhere. That is R5, queued separately, and it is his call to resolve the contradiction, not this ticket's.

## What this does not say

1. **Nothing here is ratified or wired in.** `ENABLE_SEQUENCE_GATE` defaults `False` in `research/downgrade.py`; every row above came from an explicit `enable_sequence_gate=True` kwarg on this script's own calls to `downgrade.score`.
2. **"An entry" is a definitional choice**, not something Austin's ballot answer settles precisely — see "What counts as an entry" above. The all-signal reading is reported as primary because it matches how `downgrade.score()` is used everywhere else in this codebase, and because it is the one that actually separates money on a non-trivial sample; the fired-only reading is reported alongside so the choice is visible, not buried.
3. **No hard cap is implemented** — see above. Ballot c3/c4 contradict each other and it is not this ticket's call to pick one.
4. **The traded book is pre-filtered by the legacy grader**, same caveat as `research/p2_threshold_sweep.md` -- only 1016 of 45175 signals were traded at all.
