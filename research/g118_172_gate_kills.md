# G118 -- the 172 gate kill

Generated by `research/g118_172_gate_kills.py` from `research/marks_pool.canonical_pool()` (347 S days) and `research/bt2y_trades_retest_on.json` (2024-09-03..2026-09-02, 498 sessions, 28 symbols).

## 1. The 227 split, rebuilt

| bucket | days | definition |
|---|---:|---|
| off-universe | 11 | symbol not among the book's 28 symbols |
| outside the session window | 44 | in-universe, but zero signal rows exist for that exact day anywhere in the book |
| **the 172** | **172** | in-universe, at least one candidate row exists, none is `traded: true` |
| (for reference) traded | 120 | in-universe, has a `traded: true` row -- not part of the 227 |

Off-universe + outside-window + the-172 = 227. Morning report says 227.

Matches the morning report exactly: 11 / 44 / 172.

`traded` here is not "first signal of the day" -- it is `status=="fired" and grade != "C"` (`backtest_week.SimTrade.counted`). A `C` that never won the day's with-trend floor promotion is `alert`, not `traded`, which is why the 172 is not all `X`-grade misses -- see the gate table below.

## 2. Detection miss vs. gate kill

Every one of the 172 has at least one candidate row -- by construction, a day with zero rows is in the 44 outside-window bucket, not here. So within the 172 there is no "no candidate generated" day; the detection-miss / gate-kill line sits ABOVE the 172, at the 227 level:

| | days | what it means |
|---|---:|---|
| detection miss | 55 | off-universe (11) + outside window (44) -- no session replayed, so no candidate could exist |
| gate kill | 172 | the 172 -- a candidate existed and something in the engine killed or capped it |

`GOVERNOR_S_CAP` does not exist anywhere in this codebase (checked: `grep -rn GOVERNOR_S_CAP` over every `.py`/`.md` file, zero hits). The real mechanism closest to that name is the portfolio-wide loss-halt below -- it is a cap, just not a per-symbol S cap.

## 3. Gate that killed each day's BEST candidate

"Best candidate" = the one closest to actually trading: rank `halted` (was grade A/B, blocked only by another symbol's losses) above `fired` (reached grade C) above `skipped_tight_stop` above `skipped_d` (never left X); ties broken by earliest time. One row per day.

| gate | S days killed | n priced | mean R (−1R/2R counterfactual) | winners | win rate |
|---|---:|---:|---:|---:|---:|
| portfolio loss-halt (another symbol's 2 consecutive losses blocked this A/B candidate) | 81 | 81 | +0.444 | 39 | 48.1% |
| HTF bias opposed | 34 | 32 | +0.763 | 19 | 59.4% |
| graded C, capped before promotion (RETEST_REQUIRED no retest) | 22 | 22 | +0.636 | 12 | 54.5% |
| B&R min-stop -- risk < max($0.10, 0.15% of price) | 11 | 10 | +0.500 | 5 | 50.0% |
| graded C, never promoted (not the day's first with-trend signal) | 7 | 7 | +0.714 | 4 | 57.1% |
| colour gate -- entry candle is the wrong colour | 7 | 6 | +1.000 | 4 | 66.7% |
| C-grade tight stop -- _min_viable_stop (structural floor) | 6 | 6 | -0.500 | 1 | 16.7% |
| MIN_STOP_PCT -- stop under 0.08% of price | 4 | 4 | +1.250 | 3 | 75.0% |

**All 172, one row each: n=168 priced, mean R +0.550, win rate 51.8%.**

Read the mean-R column as "was the gate right to kill it," not as a trading result -- n per gate is thin (see counts above), the counterfactual is the plain −1R/2R version Austin asked for (no ladder, no −1.25R floor, no breakeven), and every row here overlaps with every other dropped-S measurement already in this repo (`g4_dropped_s.md`, `g85_recall_honest.md`) on the SAME underlying signals, just a different denominator (his S marks, not the whole S-graded book).

## 4. Setup type

Counts any candidate of that setup appearing among the day's rows (a day can have more than one setup type fire).

| setup fired on the day | S days |
|---|---:|
| break-and-retest only (no OCR, no 84%, no confluence) | 115 |
| BR+OCR confluence tag present | 165 |
| one-candle-rule (OCR) candidate present | 52 |
| 84% re-entry candidate present | 7 |
| **OCR or 84% candidate present (either)** | **57** |

Setup of the day's BEST candidate specifically (the row the gate table above actually charges):

| setup | days |
|---|---:|
| break_and_retest | 159 |
| one_candle_rule | 13 |

## 5. Replay join-rate (skipped_d subset only)

HTF bias opposed is recomputed straight off the row (`bias` field is the exact `htf_bias` `grade_trade` saw) -- no replay needed for it. Everything else on the X-graded rows needs candle OHLC, which the exported book does not carry, so it goes through the restricted `g4_dropped_s` replay.

| | rows |
|---|---:|
| skipped_d rows across the 172's candidates | 1315 |
| ...attributed straight from `bias` (no replay needed) | 708 |
| ...needing the replay | 607 |
| ...matched | 607 |
| ...unmatched (`skipped_d, unmatched to replay (unattributed)`) | 0 |
