# TASKS — the queue

Rules of this file: one line per task, newest work at the top of its section, and
**nothing lands in Done without a commit hash and the number that moved**. Lanes are
defined in `DIRECTION.md` (green = run it unattended, amber = run it and flag it,
red = needs Austin).

---

2026-09-03: history rewritten to drop two >100MB JSON books (bt2y_trades.json, bt2y_trades_published_fill.json) that GitHub refused; every local commit from eb4f4f8e onward has a new hash; pre-rewrite tip preserved as tag backup/main-pre-filter-2026-09-03. Files are on disk, ignored, regenerable via backtest_2y.py.

---

## Green — unattended

| # | task | why it matters | check that proves it |
|---|---|---|---|
| G14 | **A/B `_calibration_grade`'s first-with-trend-signal-of-the-day floor.** G4 section 6: **968 of 1,016 traded signals (95.3%) are `B` only because of that floor** -- arrival order, not the grade, is what picks the book. T6 then proved a better grader buys nothing (held-out recall 3/15 -> 3/15, money -0.1289R), so the selector is the untested lever and the grader is not. | Every grader attempt to date has tuned something that does not select. This is the thing that does. | two arms, one table, held-out S recall and false fires reported BEFORE the in-sample numbers |
| G13 | **ANSWERED 2026-08-28 by W3 (`research/w3_recall_gate_fix.md`), flag OFF.** `ENABLE_MIN_RISK_FILL_CLAMP=1` makes `python research/regression_gate.py` exit 0 -- `s_grade` 5 -> 13, 0 dropped, on a book that is 1.1% untakeable (HEAD 2.2%, G13's structural floor 73.3%). What is left for Austin is the decision, not the code: flipping it changes what trades and re-freezing VOIDS the forward book. **Held-out S recall does not move (3/15 -> 3/15) and false fires go 12/42 -> 21/42**, so this removes a self-inflicted recall bug and buys no eye. | The instrument that guards the OMEN 6 recall gate was failing unnoticed. | `python research/regression_gate.py` exits 0 -- GREEN with the flag, RED without |

## Amber — do it, then flag

| # | task | why it matters |
|---|---|---|
| A4 | **P(reach 2R), close-fill vs intrabar-fill.** Austin 2026-08-27 tied the simple exit to the entry: the names that don't fit `flat_2r` are the ones where entering as the candle forms buys the better 2R probability. That is a testable claim and the headline metric of the side book. | It joins R6, R9 and G3 into one number instead of three separate reports. |

## Red — needs Austin

| # | task | the question only he answers |
|---|---|---|
| R1 | Grade the outstanding deck / master homework. | The only unrecoverable input in the project. |
| R2 | Ratify or reject the S/A/C thresholds after A1. | "Is this chart an S?" — nothing else can answer it. |
| R3 | **MEASURED 2026-08-27, and the answer looks like no** (`research/r3_downgrade_grader_ab.md`, `ed09f53c`): zero held-out recall gain, more false fires, -0.1289R, S durability 23/25 -> 21/25. Wired behind `ENABLE_DOWNGRADE_GRADER`, default False. | Flip it anyway, or accept that the grader is not the selector and go after `_calibration_grade` (G14)? |
| R4 | `INCLUDE_SPY_IN_BACKTEST` — SPY is 30 of his 120 graded symbol-days but excluded from `CORE_SYMBOLS`. | Q12 in the Q&A queue, still open. |
| R8 | **ANSWERED 2026-08-28: "out on that same close."** A stop is triggered by a candle CLOSE and by nothing else, the entry candle's own close counts, and one bar has one close -- so a stop cannot fire inside the entry bar ahead of the back-dated fill. That retires the wide error bar repo-wide (W0). The measurement behind it, from 2026-08-27, was already small: T2+T3: the residual intrabar ambiguity is **2 rows of 913**, not the 86.8% headline -- 790 of 792 ambiguous bars are the stop sitting on the entry bar's own extreme by construction, and 21 of the last 23 are half-cent rounding. **Tick data buys almost nothing here.** What is left for Austin is the wider question T2 exposed: the stop is derived from the entry bar, so it is not independent of it. | Is a stop built from the entry bar's own extreme the stop he actually uses? |

---

## Diagnosed, awaiting a decision

**G9 answered, and the answer is no again (2026-08-26).** `research/p10_structure_trail.md`:
14 structure-trail policies x two clock arms over the same 1,016 signals. Longs ride while
1-minute structure holds and exit on a CLOSE below the last confirmed swing low (the
`omen_bot.py::MarketStructure` fractal). **Nothing beats the incumbent** — best whole book
+0.914R vs ladder B's +0.957R, best on S +1.267R vs +1.283R. Weight taken at the HOD rung
beats weight left on the trail monotonically (+0.897 → +0.906 → +0.914, pure trail +0.827),
4R/5R partials subtract, and the noclock arm is worse for every variant. **The new number is
the ceiling**: a stop-respecting hindsight oracle returns **+3.501R** inside the 11:00
window, so 2.0R of room exists and the incumbent captures 27.3% of it. The 473 losing
signals average +0.647R of oracle and 33.8% offered +1R or better before dying — winners and
losers are not separable from price alone. The exit family is closed; the constraint is
information at entry.

**G7 answered, and the answer is no (2026-08-26).** `research/g7_exit_sweep.md`: eight exit
policies x two clock arms over all 1,016 traded signals. **Nothing reaches 2.0R.** Best on
the whole book is `30_30_30_10` at +0.955R against the incumbent ladder-B's +0.957R — the
incumbent is already the top of this family. Best on S is `flat_5r / noclock` at +1.383R
against +1.283R, +0.10R on n=128, inside the noise. Two structural reads: removing the
11:00 force-flat makes **every trailing policy worse** (the trail gives back more after
11:00 than it captures), and mean R rises monotonically with a fixed target
(1R +0.506 → 5R +0.913) without ever catching the scale-out ladder, while win rate falls
77.3% → 39.8%. **The exit is not the binding constraint; entry selection is.** What was not
tested is in the report and became G9.

**The legacy grader is wick shape, and it is not even the selector (2026-08-26, G4).**
`research/g4_dropped_s.md` attributes all 7,219 dropped S signals to the gate that
actually rejected them, by re-running `_grade_pa` over each entry bar in an instrumented
replay: **HTF bias opposed 3,525 · colour gate 2,120 · B&R min-stop 1,385 · OCR min-stop
153 · OCR wide-stop 21 · join collisions 15.** Only two of those are the grader. 303 rows
fail on the colour line *and nothing else*; delete that one `if` and 142 of them reach the
tradeable `B` tier. Austin, unprompted: "the candle doesn't have to be so specific —
you're just looking for PA to support your thesis."

**Correction carried by this ticket:** an earlier note here claimed `at_key_level` was
hardcoded to the opening range, so a PDH/PMH/pivot retest was invisible to the grader.
**That is false** — every `grade_trade` call site passes the level the setup actually
broke; the parameter is merely still *named* `or_high`. `research/t62_veto_autopsy.md`
corrected this on 2026-08-23 and G4 reproduces it: OR levels drop at 96.6%, everything
else at 96.4%, a 0.1-point gap. The 6,040-of-7,219 non-OR figure is real but is coverage,
not blame.

**And the grader is not what picks the trades.** 968 of the 1,016 traded signals (95.3%)
are `B` only because of `_calibration_grade`'s *first with-trend signal of the day, inside
90 minutes* floor. `_grade_pa` is effectively binary — `C` (alert) or `X` (silent) — and
the engine's real entry rule is arrival order. G7 said the exit is not the binding
constraint; G4 says the grade ladder is not either.

**The S supply is 58x bigger than the book (2026-08-26).** Over two years the downgrade
grader scores **7,485 signals as S**. The engine trades **128** of them. The other 7,225
are graded `X` by `_grade_pa` and dropped — 96.5% of Austin's S setups thrown away by a
candle-shape test. Split: 6,200 B&R, 1,025 OCR. This is the recall gate in one number, and
it says the shortage of S trades is a **grading** problem, not a market problem. Caveat
before anyone reads 7,485 as 7,485 trades: `NO_REPEAT_ENTRIES` scoping and the guessed
thresholds in `downgrade.py` (A1) both cut that number, probably hard.

**The 2.0R money gate is a runner problem, not a stop problem (2026-08-26).** Current book:
538 wins averaging **+2.669R** (median 2.193, max 14.26) against 473 losses at −1R. Mean R
+0.957. Solve `W x A - (1-W) = 2` on that book: at today's 53.2% win rate wins must average
**+4.64R**; at the S-grade 66.4% win rate they must average **+4.52R**. Capping losses
tighter than −1R cannot close the gap — there is no −1.25R tail to trim, the worst traded
outcome in two years is −1.000R. Austin called this before the arithmetic did.
`research/exit_lab.py` forces flat at 11:00 ET, which is the first thing to A/B (G7).

**G2 answered: the branch is deleted, and the rule is not backtestable (2026-08-26).**
`7979a61e`, `research/p8_scratch.md`. Instrumented over **43,374 created trades**: the entry bar's
close is on the good side of `sig["stop"]` and of the retested level **every single time**,
closest approach **+0.0001 bar-ranges**, zero crossings. It is not a threshold wanting
widening — the condition is consumed upstream, the same shape as `break_then_rejection` in
`research/p2_threshold_sweep.md`. **Why:** `Trading-Bot-Rulesets.md` clause 2 states the
rule as a *live fill correction* — Austin commits mid-candle without knowing the close and
scratches when the guess is wrong. The backtest never makes that guess: it reads bar `i`
complete, requires a close through the level, and only then back-dates the fill via
`fill_price`. `detect_break_retest`'s `no_confirm_close` return IS the scratch, taken
before the fill instead of after it. Branch deleted; the book is **byte-identical**
(0 of 45,175 rows differ across 15 fields), which is itself the proof it never fired.
**The nearest expressible rule was built and measured**: `ENTRY_SCRATCH=level` (default
OFF) scratches when the bar AFTER entry closes back through the retested level. It costs
**−107.06R** (+972.38 → +865.32, mean +0.9571 → +0.8517) because it cuts **70 eventual
winners** with the 185 losses, while the printed win rate *rises* 8.7 pts (53.2% → 61.9%)
purely on the shrunken denominator. Keep it off. **The keeper finding:** whether the bar
after entry holds the level splits the book **+1.3097R (n=759) vs −0.0844R (n=257)** — a
1.39R spread, sharper than anything the grader does, though only knowable one bar late.
Also queued: nothing in the LIVE path implements the rule either — `paper_trader.py` marks
on wicks and has no scratch outcome at all, and obeying clause 2 there needs an intrabar
quote, not a 1-minute bar.

**G1 answered: the gate is the bottleneck, and opening it buys nothing (2026-08-26).**
`research/p7_84_rule.md`: three arms of the arming gate over the 500-session replay.
Funnel, counted stop-outs -> arming setup -> grade gate -> before 11:00 -> signal:
strict **473 -> 472 -> 7 -> 5 -> 3**, loose **521 -> 472 -> 472 -> 433 -> 116**,
S-grade **477 -> 472 -> 43 -> 39 -> 12**. So the rule is dead in backtest because of
the GATE, not the detector — 7 of 472 opportunities survive it. The non-84% book is
identical to the cent in all three arms (1,013 trades, +966.17R), so every delta is
the re-entries alone. Whole book: strict +0.957R / 53.2% / 23-of-25 months green,
loose +0.942R / 52.1% / 25-of-25, S-grade +0.947R / 53.0% / 23-of-25. **Keep
`RULE84_STRICT=1`.** The loose re-entries are positive (+0.792R, n=79) but below the
book's own mean so they dilute; median −1.000R, 49 of 79 lose; and the 25/25
durability headline is one trade deep — drop the best re-entry from 2025-06 and it is
red again. The S arm is the worst (−0.073R) on n=7, a sample-size result rather than a
verdict on Austin's ladder. P11 row A8 agrees independently from the corpus side, and
it contradicts the LOOSE arm, not the shipped one. Next bottleneck is the detector:
433 armings produced 116 signals, and the 317 that never fired are un-autopsied.

## Done

| date | task | commit | number that moved |
|---|---|---|---|
| 2026-08-29 | T0 - land Austin's RATIFIED table R1-R27 and re-baseline the two years (`research/t0_ratified_rebaseline.md`, `research/t0_rebaseline.py`, `research/t0_heldout_recall.py`, `research/test_t0_ratified.py`, `research/test_t0_disaster_stop.py`) | _this commit_ | **The book got bigger and more durable and worse per trade.** traded 1,017 -> **2,595**; mean R +0.8341 -> **+0.5481** (move -0.2860R against a +/-0.1725R bar -- REAL, not null); win rate 53.1% -> **43.1%**; total R +848 -> **+1,422**; months green 23/25 -> **25/25 (the durability gate is MET for the first time)**; PF 2.50 -> 1.97; max DD 14.9R -> 32.4R. **Held-out S recall did NOT move: 18/34 = 52.9% before and after, the same 16 misses card for card**, and **0 of his 9 S/A veto cards fire with every ratified gate removed** -- so T10 cannot be built as a lift. Two of the gates he deleted were already dead: the counter-trend cap actually capped **9 of 45,193** signals (0.02%) and `LEVEL_BLOCK_CAP` **37** (0.08%) -- the 4th and 5th instance of the unreachable-rule bug class. The disaster stop alone is **-0.1342R** of the fall (bar +/-0.1331R, clears by <1%), and at `DISASTER_STOP_R=1.0` it rests ON the level-stop price, so **1,462 of 1,468 losses exit at the stop and a wick now stops him out** -- the one thing in T0 blocked on Austin. `python research/regression_gate.py` PASSES (any_signal 75 -> 80, nothing went silent) |
| 2026-08-28 | W3 - the recall gate turned green behind `ENABLE_MIN_RISK_FILL_CLAMP`, default False (`research/w3_recall_gate_fix.md`, `research/w3_recall_gate_fix_ab.py`) | _this commit_ | **All three done criteria met.** `regression_gate.py` exits **0** (`s_grade` 5 -> 13, all six of G12's marks back, `QQQ|2025-02-25|16` included -- the one G13 could not reach, because a clamped fill carries floor-sized risk 0.7750 against `_min_viable_stop`'s 0.5633). Untakeable **1.1%** (27 of 2,435) against the <5% target and G13's 73.3%; max R +19.9, 0 rows with `entry == stop`. **Held-out S recall 3/15 -> 3/15 -- ZERO -- while false fires go 12/42 -> 21/42**, the fourth arm in two days to buy in-sample recall and no held-out recall. Whole book n 1,017 -> 2,435, mean R +0.9551 -> +0.9635, months green 23/25 -> 25/25; matched-row delta +0.0079R on 874 rows, INSIDE the narrow bar (±0.0095R) so its sign is not established. Flag-off book byte-identical by sha256 over all 45,193 rows. The mechanism: keep the floor and the sizer on ONE number and clamp the FILL, rather than G13's moving the floor's denominator away from the sizer's. Variant B (clamp before `intrabar_stop`, restoring the structural stop and greening `spec0b_levels_check.py:44`) was built and rejected: it drops 588 of HEAD's trades, 584 takeable, and turns the matched median from +0.7870R into a full stop-out |
| 2026-08-28 | G15 - **CLOSED BY AUSTIN, not by a rig.** "The method cannot resolve its own arms" rested entirely on the +/-1.5799R WIDE error bar, and that bar existed only because nobody had ruled on whether a stop resting inside the entry bar could have fired before the back-dated fill. Asked directly, he answered: **"Out on that same close."** A stop is triggered by a candle CLOSE and by nothing else, the entry candle's own close counts, and there is exactly one close per bar -- so it cannot fire *inside* the entry bar ahead of the fill. The 790-of-792 `intrabar_stop` class is not ambiguous. W0 corrected the framing everywhere it was quoted (`research/omen6_backtest_truth.md` section 4, `g3_onwatch_2y`, `g13_floor_fix_ab`, `r3_downgrade_grader_ab`, `r9_simple_book`, `h1_2y_nowatch`, `p26_intrabar_ambiguity`). **No rig was re-run and no number was recomputed; every historical figure is kept and labelled retired.** | `52b82d9a` (the W0 files were swept into a concurrent W2 commit; the correction is that tree) | **The carried bar is now +-0.0095R (shipped arm) / +-0.0088R (ON-WATCH-off arm), 167x narrower.** Every A/B that was reported as unreadable now CLEARS it: `ON_WATCH` **+0.1135R = 12x**, `ENABLE_STRUCTURAL_RISK_FLOOR` **+0.1898R = 20x**, `ENABLE_DOWNGRADE_GRADER` **-0.1289R = 14x**, and the whole S-over-C span **+0.4035R = 42x**. The signs are readable; what they are not is LARGE -- the biggest of them is a fifth of an R on a book 1.0449R short of the gate, and all three flags bought **zero** held-out S recall. So the decision rule G15 asked for stands: **gate on held-out recall, and read mean R as a tiebreak.** |
| 2026-08-27 | T9 - THE ARTIFACT: one provenanced document that supersedes `backtest_report.md` (`research/omen6_backtest_truth.md`) | `b82e50da` | **+0.9551R (n=1,017) against the 2.0R gate -- FAIL by 1.0449R. 23/25 months green against an every-month gate -- FAIL. No pool passes**, and both are ceilings because the fill ambiguity is one-directional. Every figure carries its script and commit inline; the two cells no committed rig publishes (per-pool win rate and months-green for the shipped ladder) are left EMPTY and listed as a gap rather than estimated |
| 2026-08-27 | R3/T6 - `downgrade.py` wired in as the grader behind `ENABLE_DOWNGRADE_GRADER`, default False (`research/r3_downgrade_grader_ab.md`) | `ed09f53c` | **Do not flip it.** Held-out S recall **3/15 in BOTH arms -- zero gain** -- while false fires go 12/42 -> 14/42; of the four days it switches on (1 A, 1 C, 2 X) none is a day Austin graded S, and the recall gate moves -0.086 -> **-0.133**. Money n 1,017 -> 1,310, takeable mean R +0.9716 -> **+0.8427** (delta -0.1289R -- inside the RETIRED wide bar, but it clears the carried narrow bar of +-0.0095R by 14x and 22 of 27 symbols move the same way; the 350 added trades earn +0.398R against the book's +0.955R). Months green 23/25 -> 24/25 whole book, **23/25 -> 21/25 on S** -- wins one durability read, loses the other. Flag-off book byte-identical by sha256 over all 45,193 rows |
| 2026-08-27 | R9/T10/A4 - the simple side book and Austin's P(2R) claim (`research/r9_simple_book.md`) | (this session) | `flat_2r` standalone: **+0.6997R, 24/25 months green** -- fails the gate by 1.3003R and books **0.2554R LESS** than the incumbent ladder on the identical 1,017-trade set. **The intrabar fill does not raise P(2R); on a fixed denominator it halves it** -- 36.00% close-fill vs 14.29% back-dated over the 175 signals the extra class can reach. The seductive 62.50% is survivorship: 135 of those 175 never enter the book at all (the earlier fill puts them on or through their own stop) and under the close fill they book -0.0058R -- flat, not a saved tail. And **R is denominated in |entry - stop|, which back-dating shrinks to a median 63%, so "2R" in that arm is a 37% smaller price move**. Names that do not fit flat 2R (n >= 20): AMD, AMZN, META, MSFT, NVDA, PLTR; fits: GOOGL, TSLA |
| 2026-08-27 | G12 - bisecting the red recall gate (`research/g12_recall_regression.md`, `g12_attribute.py`) | `df8e1c89` | Culprit **`5e3677ea`** (2026-08-11, omen-5.0 T3/T4/T10/T11/T12), a one-step bisect transition confirmed deterministic both sides. T3(b)'s `fill_price` (`signal_runner.py:677`) takes the entry **at the broken level** instead of at the close; **for break-and-retest the level IS the stop**, so `stock_risk` collapses and the pre-existing minimum-risk floor at `:1657`/`:1892` forces `TradeGrade.D`. All six dropped marks satisfy `risk_after < floor <= risk_before`. **Bug, not a trade-off**: `intrabar_stop` was shipped as a guard against exactly this and recovers 0 of 6. Reverting the fill returns `s_grade` 5 -> 13 with all 75 detections kept, so P16/P17/`b55bd9c9`/`_grade_pa` are all gate-neutral |
| 2026-08-27 | G13 - the structural-floor fix measured behind `ENABLE_STRUCTURAL_RISK_FLOOR`, default False (`research/g13_floor_fix_ab.md`) | `6d89513d` | **It fixes the gate and not the thing the gate stands for.** `s_grade` 5 -> 11 (13 is G12's number for reverting the fill outright); five of six fire, `QQQ|2025-02-25|16` is then killed by `_min_viable_stop` (0.4900 vs 0.5633). **Held-out S recall 3/15 -> 3/15 while false fires go 12/42 -> 19/42 -- not one of the 12 days it switches on is a day Austin graded S.** Money unusable as booked (+13.7619R): 1,139 of 1,553 on-arm rows sit under the moved floor, 79 with `entry == stop` exactly and max R **+7,100**, because `backtest_week` sizes at `RISK_DOLLARS / |entry - stop|` post-fill while the floor now reads pre-fill; defensible delta **+0.1898R on 429 matched rows -- 8x inside the RETIRED wide bar, 20x OUTSIDE the carried narrow bar of +-0.0095R**. The SHIPPED book is clean -- 0 rows under half a cent, max R 14.264, min -1.0 |
| 2026-08-27 | T1 - OMEN Test 1 scored against the engine on 100 held-out symbol-days (`research/t70_test1_score.md`) | `30fbc3f8` | **S recall 3/15 (20%), in-universe 2/12. False fire 12/42 (29%)** -- on days it has never seen the engine is MORE likely to fire on a setup Austin refused than on one he called S. Entry-bar agreement within +/-2 bars **4/58 (7%)**, so the day-level hits are mostly right-day-wrong-bar. 16 cards (IWM 8, ACHR 5, SPY 3) are on symbols in no backtest tier and are reported separately, not as misses. Engine tier `A+` never occurs in the sample, so that column of the ladder mapping is empty by construction |
| 2026-08-27 | T2/R8 - the intrabar ambiguity rate (`research/p26_intrabar_ambiguity.md`) | `8bb78c77` | 86.8% of traded intrabar fills sit on a bar whose range also holds the stop; spread **1.5815R** (+0.9571 optimistic vs -0.6244 pessimistic) against a 1.0429R gap-to-gate and a 0.4035R S-over-C edge -- at the time, **every mean-R A/B in the book reported a number smaller than this error bar**. **That spread is the WIDE bar and it was RETIRED 2026-08-28** (Austin: "out on that same close"); the carried bar is the narrow +-0.0095R and every one of those A/Bs clears it by 12-42x. But 790 of 792 are the stop sitting ON the entry bar's own extreme, placed by `signal_runner.intrabar_stop`; T3 then showed 21 of the remaining 23 are half-cent rounding, so **genuinely residual ambiguity is 2 rows of 913**. Does not flip the gate -- one-directional, so +0.9571R is a ceiling. Also found: the book stores entry/stop at 2dp, so a naive `entry != close` test over-reports by ~11 points |
| 2026-08-27 | T3/G3 - `ON_WATCH` A/B on the 2-year rig, not the 120 day-cards (`research/g3_onwatch_2y.md`) | `47e60796` | **The flag does not do what its name says**: `ON_WATCH` gates only `near_session_extreme`, reachable from 2 of `fill_price`'s 10 call sites; `bar_extreme_veto` is never gated, so `ON_WATCH=0` leaves **74.7% of fills still intrabar** and no close-fill arm is expressible through it -- which is why t61's day-card A/B measured +0. Arms: OFF n=1091 **+0.8416R** 54.1% **24/25 months green** - ON (shipped) n=1017 **+0.9551R** 53.2% **23/25**. Delta +0.1135R book / +0.2023R S -- 14x below the wide error bar (RETIRED 2026-08-28), **12x above the narrow bar, which is the one carried since**, so the sign is readable. **The shipped arm buys mean R by giving back a green month**, and every-month-green is the durability gate |
| 2026-08-27 | A1/T4 - the eight downgrade thresholds swept on three rigs (`research/a1_threshold_sweep.md`, `p2_threshold_sweep.py --a1`) | `99bead1c` | 1 of 8 wrong-signed: `level_not_respected` again (tripped +0.996R n=638 vs clean +0.892R n=378), 63-68% trip rate on both rigs, so it is the biggest driver of the C bucket while pointing the wrong way -- P15's third confirmation, still a STOP not a retune. **The grader is overfit and the held-out set proved it**: mix distance from Austin **0.086 on the 120 cards it was tuned against, 0.282 on the held-out 100**, A undercounted 3x, S-day recall 5/15 |
| 2026-08-27 | A2 - every published headline number re-run on the 2-year rig (`research/a2_figure_refresh.md`, `a2_bt2y_summary.py`) | `d4cf3710` | **16 of 18 moved, 0 UNREPRODUCIBLE** -- every number traces to a committed script, so the failure mode is staleness, not missing code. Largest delta: `t60_baseline.md`'s money-gate read, published **+0.0787R (FAIL)** vs **+0.9551R** today, because its Corpus B is the 12-month yfinance rig and not the 2-year archive -- **so +0.0787R and +0.957R were never the same measurement** and have been quoted side by side. No published file edited |
| 2026-08-27 | A3 - the S-per-symbol-day cap swept over {none, 1, 2, 3}, shipping nothing (`research/a3_s_cap_sweep.md`) | `615a0ce3` | **No cap.** S recall (5/15) and false fire (30/42) are byte-identical across all four arms on the held-out rig, because a trade-count cap can only remove a symbol-day's 2nd-or-later S entry and never the first -- **it is structurally invisible to a day-level recall metric**. On the book cap `1` has the best raw mean R (+1.432R n=106 vs +1.313R n=129) but its 95% CI overlaps every other arm and it gives up 10.4% of total realised R. Austin called this before the sweep did |
| 2026-08-27 | G10 - the 317 armings that never fired, autopsied (`research/g10_arming_funnel.md`) | `66557d12` | `rr15` (remaining reward >= 1.5x risk) kills the most alone -- **92 of 318, and 100% of its bucket is rescuable**, unlike every other condition. Replayed with real close-based, -1.25R-floored exits those 92 go 52W/39L, **mean +0.617R** -- below the book's own +0.957R, so opening it buys nothing and `RULE84_STRICT=1` stays. **`before11` is dead code**: `ENTRY_CUTOFF` already excludes every bar it could reject (4th instance of the unreachable-rule class). Count is 434/318 not P7's 433/317, traced to a 2026-08-21 archive backfill and verified benign by asserting shadow-fired == the engine's own 116 |
| 2026-08-27 | G11 - clause 2's scratch in the LIVE path, scoped (`research/g11_live_scratch_scope.md`) | `00d64ad5` | **A genuine data defer, not a code gap**: the `scratch` outcome is a boundable change at `paper_trader.py:50-288` / `live_scanner.py:356-372,508-586`, but Tastytrade DXLink polls 1-minute bars every 60s and Polygon has no realtime entitlement and is not wired into the live path at all. Surfaced the wick finding below |
| 2026-08-27 | **The live path was stopping out on WICKS** -- `paper_trader.py::_check_stop` and the Rule 6 runner arm in `exit_for`, plus the `live_scanner.py:359` caller that only ever passed high/low. One shared `stop_rule.py::stop_hit_on_close` that `backtest_week.py` also wraps | `76a15fce` | Contradicted the rule Austin settled five times (*stops trigger on the CLOSE, wicks stop nothing*), with no data dependency and a correct reference implementation one file over. New `research/test_paper_trader_stop.py` is **red at 7 of 18 against the old bodies, green now**; book **byte-identical** (3,845 trades to the same digest, plus 800k random bars across both `STOP_ON_CLOSE` arms). `paper_trader.py`'s own self-test had been **dead on a `NameError`** and asserting on wicks. No -1.25R floor added live: the fill stays at `stop_premium`, exactly 1R however far the close overshoots, so a floor would be an unreachable branch -- pinned as a test property instead |
| 2026-08-27 | G6/T5 - one per-symbol sample floor (`universe.py:131`, `research/test_sample_floor.py`) | `05436a80` | `MIN_SAMPLE_N = 20`, taken from `research/p12_sample_floor.md` which had already settled it rather than inventing a second number. Four reporting paths import it, including `t70_metric_sweep.py` whose private `THIN_N = 20` was a second copy. Reports stop printing GOOGL n=21 beside COIN n=104 as equal evidence; suppressed rows still count in whole-book totals |
| 2026-08-27 | G5 - corpus sweep for rules nobody ever stated (`research/hallucination-audit.md`) | `86d96f99` | 15 CONFIRMED / 2 CONTRADICTED / **33 UNMENTIONED**. Worst unmentioned: **`STRONG_PA_MULT = 1.5x`**, which gates the 84% reclaim and has no author; then `STOP_RANGE_MULT = 0.75x` and the S-score weights (data-derived from a 24mo split, never course-taught) |
| 2026-08-27 | T7 - the provenance gate (`research/test_provenance.py`) | `cef00981` + `1e787a6b` | A research report stating an R-multiple, a percentage or a sample size must name the script AND the commit that made it. **98 existing reports fail** and are grandfathered with a per-file reason -- the reproducibility problem measured for the first time. It caught its first three the same day (A1, G10, P26 -- all written after it landed, all naming a script and no commit) |
| 2026-08-27 | T8/R6 - the deck can record a mid-candle fill (`research/build_omen_test1.py`) | `cef00981` | `out.entry_p = closes[i]` was unconditional, so every entry in the corpus read as an at-close fill **by construction** and the question Austin most wants answered could not be answered from the data. Now the bar's close is the default, a typed price wins, `bar_close_p` keeps the close and `entered_before_close` sets itself. Four new jsdom checks. Deck id is a parameter (`OMEN_DECK`) so Test 1's graded HTML is never regenerated over |
| 2026-08-27 | OMEN Test 2 built on Austin's 12-symbol roster (`research/omen_test2.md`, `research/probes/omen-test-2.html`) | `db896c67` | 100 cards on `CORE_SYMBOLS` + `INDEX_POOL`. Quotas s_traded 12 / s_dropped 28 / silent 15 / offroster 20 / low 25; `range_fade` returns zero classifiable candidates on ANY roster, so that stratum fell 25 -> 20. `MAX_PER_SYMBOL` 8 -> 9, the arithmetic minimum (12x8=96 < 100). **758 already-judged symbol-days excluded, 0 repeats** |
| 2026-08-27 | P25 - why the mid-candle percentage could not be measured (`research/p25_midcandle_entry.md`) | `9d0c2206` | 117 entries measured. Every S/A/C entry from Test 1 reads **100% at-close**, a fact about the page and not about Austin; the 55 genuine mid-bar entries all come from the older chart-click decks. Floor from prose: **14 of 58 graded notes (24%)** say *"as candle forming not HOD/LOD"*. Median position in the entry bar 0.82-0.93, the adverse end; **the entry bar closes within 10% of the session extreme 34% of the time** and the rig fills at that close |
| 2026-08-27 | P23 — P18+P19+P20 measured TOGETHER, not alone (Block 6): `research/p23_combined_arms.py` reuses `p2_threshold_sweep.py`'s own `build_cards()`+`split_cards()` for the identical 50/50 stratified hold-out, one pass over the 2-year book for all five arms (`research/p23_combined_arms.md`). Every `ENABLE_*` flag stays `False` | `_this commit_` | HOLD gate (S recall − false-fire rate): baseline **−0.159** → P19 only **−0.159** (zero marginal, matches P19's own solo finding) → P20 only **+0.046** → P19+P20 **+0.046** (same gate as P20 alone) → P19+P20+P18 **+0.007**. Money rig, S mean R: baseline +1.313R (n=129) → P19 only +1.239R (n=147) → P20 only **+1.572R (n=77, best)** → P19+P20 +1.469R (n=91) → P19+P20+P18 +1.337R (n=57). **P20 alone is best on both rigs — cards and money agree.** Stacking P19 on top leaves the card-gate unchanged but pulls weaker signals into the S tier at signal granularity (n 77→91, mean R 1.572R→1.469R) — the overlapping-population dilution the ticket predicted. Adding P18 on top of that is worse on both rigs. S>A>C survives in every arm |
| 2026-08-27 | P20/W6 -- the sequence gate: `sequence_gate`, the 2nd+ graded entry on a symbol-day (rule ballot batch 02, b2), 84%-rule re-entries exempt, no cross-symbol effect, OFF by default behind `ENABLE_SEQUENCE_GATE` (`research/p20_sequence_gate.md`) | `_this commit_` | `NO_REPEAT_ENTRIES` does NOT already cover this -- it only blocks exact-level repeats, not any subsequent entry; population is real, not P15-dead. All-signal reading (every detected signal, matching how `downgrade.score()` is used everywhere else): trips 1130/1250 cards (90.4%), 33369/45175 book (73.9%); traded mean R tripped **+0.767R** (n=422) vs clean **+1.092R** (n=594), delta **-0.325R** -- correctly signed on a real sample. 425/1016 traded signals (41.8%) are 2nd+ on their symbol-day (3 exempt, 422 trip); S recall 12/28 -> 8/28, false fire 30/61 -> 9/61, S>A>C survives. Fired-only reading (legacy engine's own accepted subset) trips only 100/45175 (7.2%) and only 9 traded signals, wrong-signed (+0.293R delta) -- both readings reported, the choice between them is flagged for Austin, not resolved here. No hard cap implemented (ballot c3/c4 contradict each other; that's R5) |
| 2026-08-27 | P18/W4 -- a ninth downgrade: `large_counter_body` (rule ballot batch 02, b6), OFF by default behind `ENABLE_LARGE_COUNTER_BODY` (`research/p18_p19_new_variables.md`) | `_this commit_` | trips 613/1250 cards (49.0%), 25822/45175 book (57.2%); traded mean R tripped **+0.968R** (n=622) vs clean **+0.939R** (n=394), delta **+0.029R** -- near-zero and wrong-signed for a downgrade, and fires almost as often as `counter_trend_not_respected` (89.5%) already does; not a threshold to retune, a finding for Austin |
| 2026-08-27 | P19/W5 -- a second +1: `multi_level_confluence` (rule ballot batch 02, b5), six levels = PDH/PDL/PMH/PML/ORH/ORL (HOD/LOD/T10 pivots named and excluded), capped with BR+OCR at +1 total, OFF by default behind `ENABLE_MULTI_LEVEL_CONFLUENCE` (`research/p18_p19_new_variables.md`) | `_this commit_` | trips 441/1250 cards (35.3% of covered), 10801/45175 book (23.9% of covered); traded mean R tripped **+1.064R** (n=582) vs clean **+0.814R** (n=434), delta **+0.250R** -- right-signed; zero marginal effect on the day-card gate (S recall 12/28, false fire 30/61, agreement 20/58 all unchanged, since the population mostly overlaps BR+OCR's existing 64.9% card confluence rate) but S n rises 129->147 traded at signal granularity; S>A>C money ordering survives under P18, P19, both, and baseline |
| 2026-08-27 | P22 — the 11:00 boundary is manage-only (rule ballot batch 02, c7): audit on entry cutoff and runner scale-out | `_this commit_` | **no entries after 11:00**: yes, enforced at `backtest_week.py:621–622` via `ENTRY_CUTOFF="11:00:00"`, data confirms 0/45175 at/after 11:00 (latest 10:59 ET). **Runner 10%**: no — shipped default uses 50/50 split (50% HOD, 50% runner target); the 10% runner is `policy_30_30_30_10` in exit_lab.py (test only), not backtest |
| 2026-08-27 | P16/W3 — `htf_bias` has no author (rule ballot batch 02, c6); traced every producer/consumer, corpus-checked, re-ran G4 with the veto off, defaulted it OFF in the legacy grader behind `HTF_BIAS_VETO` (`research/p16_htf_bias.md`) | `_this commit_` | of the 3,525 dropped S signals the veto killed, **60 (1.7%) reach a tradeable tier with the veto off**, averaging **+1.012R** (n=60) vs the book's +0.957R and the general dropped-S baseline's +0.465R (n=588) — small sample, worth an A/B, not a result to ship on |
| 2026-08-27 | P17 — `STALE_BARS` 15 -> 10, ratified by rule ballot batch 02 (b11) | `eff5a9e9` | `stale_retest` trips 3/1250 -> 8/1250 on cards (0.2% -> 0.6%), 98/45175 -> 263/45175 on book (0.2% -> 0.6%) — near no-op, as the ticket predicted |
| 2026-08-27 | P21/W7 — is there a tracked level >= 2R from entry (rule ballot batch 02, b4)? Roster = PDH/PDL, PMH/PML, OR high/low, causal HOD/LOD, T10 pivots (`research/p21_target_availability.md`) | `_this commit_` | **the flag runs backwards from the hypothesis**: only 12.5% of the 473 losers lacked a 2R target at entry vs. 23.9% of the 543 winners; whole book has-target 827/49.9%/+1.037R vs no-target 189/68.8%/+0.608R; S subset (n=128, +1.283R) has-target 97/64.9%/+1.533R vs no-target 31/71.0%/+0.501R — negative result, not wired in |
| 2026-08-26 | OMEN Test 1 — 100-card grade/entry/stop test + the no-repeat guard's blind spot | _this commit_ | judged symbol-days **633 -> 658**: `marked_card_ids()` read `probe_master_homework_2026-08-26.jsonl` as 6 garbage keys (`cal_QQQ`, `au_TSLA`, …) because `_judgement_key` split prefixed card_ids on `_`, and could not see the 25 `sr_` rows at all (`grade: null`, judgement lives in `answers`). Both fixed; the file now contributes **49** (51 rows minus the 2 G12 duplicates). Test: 100 cards / 50 grader-S (one disagreement = 2.0 pts of the 95% target) / 25 off-roster / 0 repeats against history or in-document |
| 2026-08-26 | G8 — BR+OCR confluence as its own setup type (P3) | `b55bd9c9` | 29,815 of 45,175 detections re-labelled `br_ocr_confluence`; book identical to the cent (1,016 traded / 52.95% / +0.9571R / +972.38R). Funnel detection → grade → traded: B&R alone 13,546 → 828 → 391 (50.1%, +0.888R) · OCR alone 1,811 → 70 → 16 (18.8%, −0.315R) · confluence 29,815 → 1,353 → 606 (55.6%, +1.030R) |
| 2026-08-26 | P5 — rename `LADDER_MODE` to `SCALE_PLAN` (`"A"`/`"B"` -> `"hod_then_runner"`/`"hod_then_runner_be"`); `OMEN_LADDER_MODE` kept as a deprecated alias | `d981ec2f` | none — pure rename; 45-day smoke replay produced byte-identical trades under the old env var and the new default |
| 2026-08-26 | G4 — what the legacy grader throws away, by branch | `d8b04625` | 7,219 dropped S attributed: bias 3,525 / colour 2,120 / B&R min-stop 1,385 / OCR 174; 303 fail on the colour line alone, 142 of those reach tradeable B; dropped-set expectancy n=588 +0.465R vs book +0.957R; 968 of 1,016 trades are B only from the first-of-day floor |
| 2026-08-26 | G1 — 84%-rule three-arm A/B (P7) | `40fdadd3` | 84% re-entries 3 -> 116 (loose) / 12 (S) fired; book mean R +0.957 -> +0.942 / +0.947; recommendation: no change |
| 2026-08-26 | 2-year replay + interactive report, both grade ladders attached | `04a6e60f` + follow-up | first S/A/C-filterable book: 1,016 traded signals, mean R +0.957 vs the 2.0 gate |
| 2026-08-26 | G9 — structure trail + far-target tail (P10) | `6c3f880f` | nothing beats ladder B: best +0.914R whole book / +1.267R on S; new ceiling number — stop-respecting oracle +3.501R, incumbent captures 27.3% |
